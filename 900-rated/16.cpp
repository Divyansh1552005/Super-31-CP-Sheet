#include <bits/stdc++.h>
using namespace std;

int main(){
    int n;
    cin >> n;
    while (n--){
        string s,t;
        cin >> s >> t;

        // if we can make t from s by performing operations
        if(s == t){
            cout << "YES \n";
        }

        

    }
    return 0;
}