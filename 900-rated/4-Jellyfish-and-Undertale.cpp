#include <bits/stdc++.h>
using namespace std;

int main(){
    int t;
    cin >> t;
    while (t--){
        int a,b,n;
        cin >> a>> b >> n;

        vector<int> v(n); // tools timer array
        
        for (int i = 0; i < n; i++) cin >> v[i];
        
        

    }
    return 0;
}